name: Java CI with Maven

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4 # Cập nhật lên v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4 # Cập nhật lên v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: 'maven'

    - name: Build and Test Bai03
      run: mvn -f Bai03/pom.xml clean package 

    - name: Upload Artifact
      uses: actions/upload-artifact@v4 # Cập nhật lên v4 (Đây là lỗi chính)
      with:
        name: math-utils-build
        path: Bai03/target/*.jar
